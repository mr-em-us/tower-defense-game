{
  "name": "tower-defense",
  "version": "1.0.0",
  "description": "2-player networked tower defense game with ASCII art style",
  "type": "module",
  "scripts": {
    "build:shared": "tsc -p tsconfig.shared.json",
    "build:server": "tsc -p tsconfig.server.json",
    "build:client": "node scripts/build-client.mjs",
    "build": "npm run build:shared && npm run build:server && npm run build:client && cp client/index.html dist/client/ && cp client/styles.css dist/client/ && cp -f client/sandbox.html dist/client/ 2>/dev/null; true",
    "start": "node dist/server/server/index.js",
    "dev:server": "npx tsx server/index.ts",
    "dev:client": "npx esbuild client/main.ts --bundle --outfile=dist/client/bundle.js --format=esm --platform=browser --tsconfig=tsconfig.client.json --watch --servedir=dist/client",
    "dev": "npm run build && concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "tunnel": "npm run build && concurrently \"npm start\" \"npx localtunnel --port 9090\"",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "uuid": "^9.0.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/uuid": "^9.0.7",
    "@types/ws": "^8.5.10",
    "concurrently": "^8.2.0",
    "esbuild": "^0.19.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^4.1.1"
  }
}
